# The stage of the value-ownership plan that completes each line JGraph fails (appendix A's stage column).
v_global_prop_struct_field_write	V6	# a struct held by an object's property takes no field write, local or global (#161)
x_global_gfx_prop_elem_write	V6	# an indexed write into a graphics property is dropped, local or global (#95)
